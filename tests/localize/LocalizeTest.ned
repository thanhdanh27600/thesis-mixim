package org.mixim.tests.localize;

import org.mixim.tests.TestObject;

// Test network for class Coord tests.
simple LocalizeTest extends TestObject
{
    @class(LocalizeTest);
    @isNetwork(true);
}
